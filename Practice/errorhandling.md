# Type error is a datatype issue
#Name error is when we didn't declare the variable
#syntax error 
#valueError when you give invalid value like int('a')
#indexError : index out of range... when we access data that is not there
#key error: we only find this error in dictionary 
#zeroDivisionError when you try to divided any vlaue by zero
#AttribiuteError
#moduleError


from countries import countries